# Learnings

- Provider portability requires job identity to be independent of deployment
  topology.
- A capability declaration is evidence only when it is pinned to an immutable
  provider profile version and evaluated against an immutable job definition.

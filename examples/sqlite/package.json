{
  "name": "@agent-feed/sqlite-reference",
  "version": "0.1.1",
  "private": true,
  "description": "Dependency-free SQLite portability reference for Agent Feed producer lifecycle invariants.",
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "test": "node --experimental-sqlite --test --test-concurrency=1 test/*.test.mjs",
    "demo": "node --experimental-sqlite bin/demo.mjs",
    "verify": "npm test && npm run demo"
  }
}

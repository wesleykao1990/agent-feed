# Agent Feed PRD v0.1.1

The canonical PRD is `docs/00_product_requirements.md`. It defines the generic run/finding/evidence product, expected-run liveness, trust boundary, thin first implementation, and deferred platform work.

The implementation sequence is `docs/03_implementation_plan.md`. The runnable baseline is under `prototype/`.

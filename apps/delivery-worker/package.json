{
  "name": "@agent-feed/delivery-worker",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "README.md",
    "package.json"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "start": "node --experimental-strip-types src/main.ts",
    "test": "node --experimental-strip-types --test test/*.test.ts"
  },
  "dependencies": {
    "@agent-feed/delivery-core": "file:../../packages/delivery-core",
    "@agent-feed/persistence-postgres": "file:../../packages/persistence-postgres",
    "@agent-feed/protocol-runtime": "file:../../packages/protocol-runtime",
    "@agent-feed/webhook-adapter": "file:../../packages/webhook-adapter"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "typescript": "7.0.2"
  }
}
